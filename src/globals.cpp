#include "carbon/globals.hpp"

namespace carbon {
    renderer::buffer* buf;
    renderer::text_font* main_font;
    renderer::text_font* title_font;

    renderer::performance_counter performance;

    style_sheet theme;

    benchmark_data benchmark;

    context_data context;
}// namespace carbon